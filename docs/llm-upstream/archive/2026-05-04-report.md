# LLM 上游差异报告 2026-05-04

> Historical audit note: this file is an archived baseline report. It contains old statuses that may conflict with current implementation state. Current status is maintained in [../status.md](../status.md), and new sessions should start from [../session-brief.md](../session-brief.md).

## 基线

- 当前仓库：`/home/fyl/opencode`
- 当前分支：`feat/llm-system-smoke-tests`
- 当前 HEAD：`b2f150759`
- 上游仓库：`/home/fyl/tmp/opencode`
- 上游分支：`dev`
- 上游 HEAD：`28112fbd1`
- 上游时间窗口：`2026-03-26` 之后进入 `dev` 的 LLM/session/provider/config/auth/package 相关提交

当前工作区已有变更未触碰：

- `packages/opencode/src/provider/models-snapshot.js`
- `packages/opencode/src/session/llm.ts`
- `packages/opencode/test/session/llm.test.ts`
- `docs/llm-upstream-diff-migration-plan.md`
- `aether-site/`

## 方法

本轮按 `docs/llm-upstream-diff-migration-plan.md` 的第 1 步执行，只生成报告，不改运行时代码。

已执行的分析：

- 对比当前仓库与上游 clone 的 `packages/opencode/src/session`、`packages/opencode/src/provider`、`packages/opencode/src/config` 的 no-index diff。
- 抽读高价值文件差异：`session/llm.ts`、`session/prompt.ts`、`session/retry.ts`、`provider/provider.ts`、`provider/transform.ts`、`provider/models.ts`。
- 扫描并抽查上游 `2026-03-26` 之后相关 commit，重点看 bugfix、provider 兼容、reasoning、tool call、usage、AI SDK/provider SDK bump。
- 对照当前 Aether 的本地兼容层和测试，避免把已覆盖或本地策略误判为待回迁。
- 在当前 HEAD `b2f150759` 复核 P0/P1 项，确认多项此前标为缺失的行为已经由现有工作区改动或本地实现覆盖。

## 候选项

| 编号 | 优先级 | 上游 commit/PR | 变更主题 | 影响 provider/model | 当前 Aether 状态 | 建议动作 | 风险 | 测试要求 | 验收命令 |
|------|--------|----------------|----------|---------------------|------------------|----------|------|----------|----------|
| LLM-UP-001 | P0 | `a12333310` / `#25145` | `providerOptions` key 对 OpenAI-compatible dot provider 做 split | OpenAI-compatible custom provider，如 `wafer.ai` | 已覆盖；`ProviderTransform.providerOptions` 已对 OpenAI-compatible/openai/anthropic dot providerID 取首段 | 已验证 | 继续避免影响 xAI/Mistral/Groq 等硬编码 key | 已有 dotted providerID 断言 | `cd packages/opencode && bun test test/provider/transform.test.ts` |
| LLM-UP-002 | P0 | `348a84969` / `#22646` | Anthropic assistant content 中 `tool-call` 后接 text/reasoning 时重排，避免 `tool_use` 未立即跟 `tool_result` | Anthropic、Google Vertex Anthropic | 已覆盖；`ProviderTransform.message` 已拆分 `[tool-call, text/reasoning]`，`message-v2.ts` 已处理 pending/running tool | 已验证 | Anthropic 历史消息顺序敏感，需避免影响非 Anthropic provider | 已有 transform 与 pending/running tool 回归测试 | `cd packages/opencode && bun test test/provider/transform.test.ts test/session/message-v2.test.ts` |
| LLM-UP-003 | P0 | `733a3bd03` / `#14973` | provider 返回 `stop` 但 assistant 含 tool call 时继续工具循环 | OpenAI-compatible | 已覆盖；`LLM.stream` middleware 已将含 tool call 的 `stop/unknown` 归一为 `tool-calls` | 已验证 | 后续改 streaming middleware 时需保留该归一化 | 已有 fake endpoint 覆盖 finish=`stop` 且含 tool call 的继续执行 | `cd packages/opencode && bun test test/session/llm.test.ts` |
| LLM-UP-004 | P0 | `4ca809ef4` / `#22511` | 5xx API error 即使 `isRetryable` 未置位也应 retry | 所有 provider | 已覆盖；`SessionRetry.retryable` 已对 `statusCode >= 500` 放行，并保留 context overflow 不重试 | 已验证 | 可能增加真实 5xx 重试次数，符合上游修复语义 | 已有 retryable 5xx/4xx/context overflow 断言 | `cd packages/opencode && bun test test/session/retry.test.ts` |
| LLM-UP-005 | P0 | `29ec07700` / `#25303` | 不同模型间转换 reasoning part 时改为普通 text，修复 Bedrock reasoning 问题 | Bedrock、跨模型继续对话/切模型 | 已覆盖；`MessageV2.toModelMessages` 已在 `differentModel` 时把非空 reasoning 转 text 并丢弃空 reasoning | 已验证 | 跨模型 reasoning 转 text 只应在 `differentModel` 时生效 | 已有 different-model reasoning 转 text 断言 | `cd packages/opencode && bun test test/session/message-v2.test.ts` |
| LLM-UP-006 | P0 | `86715fecc` / `#24180`、`923af96d2` / `#24146` | DeepSeek assistant 历史始终带 reasoning，并保留空 `reasoning_content` | DeepSeek V4 thinking、OpenAI-compatible interleaved reasoning | 已覆盖；DeepSeek assistant 会补空 reasoning，并通过 interleaved 回灌为空 `reasoning_content` | 已验证 | Aether 本地 DashScope/ZAI/Kimi/GLM 逻辑必须继续保留 | 已有 DeepSeek 非空与空 reasoning_content 断言 | `cd packages/opencode && bun test test/provider/transform.test.ts` |
| LLM-UP-007 | P0 | `e7053c41f` / `#24435` | 升级 `@openrouter/ai-sdk-provider` 修复 DeepSeek reasoning，并跳过本地 interleaved 回灌 | OpenRouter DeepSeek | 已升级；随 AI SDK v6 专项升到 `@openrouter/ai-sdk-provider@2.9.0`，删除旧 `1.5.4` patch | 已迁移，待真实 smoke | 新 OpenRouter reasoning 需真实 provider 验证；旧 patch 不能直接套用到 2.x | 本地 typecheck/provider/transform/LLM/Copilot 测试已通过；仍建议 OpenRouter/DeepSeek smoke | `cd packages/opencode && bun typecheck`，再按系统 smoke 文档跑 OpenRouter/DeepSeek |
| LLM-UP-008 | P0 | `9965d385d` / `#20272`、`c5deeee8c` / `#22764`、`cb18f2ef4` / `#22957`、`a740d2c66` / `#25007`；后续 `967557979` / `#26222` | Azure providerOptions、`store`、prompt cache、reasoning defaults 对齐 OpenAI；Azure `gpt-5.5` completions API 避免发送 `reasoningEffort` | Azure OpenAI | 部分覆盖；当前测试已有 Azure chat/responses reasoning scrub，当前 `providerOptions` 保留 `azure` key；但 `#26222` 属后续新增修复，需按 `LLM-UP-021` 复核 | 保留本地策略，新增项单独复核 | 上游双写可能与当前测试“azure 不 remap 到 openai”冲突；`gpt-5.5` completions 对 reasoning 参数更敏感 | 不整块回迁；按新增 Azure `gpt-5.5` case 补契约测试 | `cd packages/opencode && bun test test/provider/transform.test.ts` |
| LLM-UP-009 | P0 | `280eb16e7` / `#21047`、`72c77d0e7` / `#19758` | reasoning/cache token 不重复计费，Anthropic/Bedrock AI SDK v6 usage 归一 | Anthropic、Bedrock、OpenRouter、Venice | 已重评；AI SDK 已升到 `6.0.174`，现有 `Session.getUsage` 归一逻辑通过 v6 本地测试 | 已验证 | 真实 provider usage metadata 仍建议 smoke 复核，尤其 OpenRouter/Bedrock | 保留 usage/compaction 单测；真实 provider 再跑 smoke | `cd packages/opencode && bun test test/session/compaction.test.ts test/session/compaction-flow.test.ts` |
| LLM-UP-010 | P1 | `f8738c900` / `#25434`、`23c865608` / `#20605`、`fff98636f` / `#20929`；后续 `16c457e71` / `#27347` | `models.dev` 服务化、schema 拆分、删除/弱化 snapshot；后续移入 `packages/core` | models.dev 元数据加载 | 暂不适合；当前 Aether 保留 `models-snapshot.js`，且该文件已有本地未提交改动；上游已进一步核心包化 | 暂缓 | 容易覆盖本地 snapshot 和模型扩展；架构重构噪音大 | 不做代码回迁；后续只抽取具体模型能力字段 | 不适用 |
| LLM-UP-011 | P1 | `7230cd268` / `#22248` | 新增 Alibaba SDK 和 cache 支持 | Alibaba/DashScope | Aether 已有本地 DashScope/OpenAI-compatible `enable_thinking` 与国产模型参数策略 | 已覆盖，保留本地策略 | 上游改为官方 SDK 可能破坏 Aether 国内网关语义 | 用现有 alibaba-cn smoke 验证 | `cd packages/opencode && OPENCODE_SYSTEM_TEST=1 bun run test:system:llm:p0 -- --provider alibaba-cn --input p1-reasoning --p1` |
| LLM-UP-012 | P1 | `0acac216a` / `#24734`、`610c036ef` / `#22824`、Copilot SDK diff；后续 `834515231` / `#25821` | Copilot GPT-5 variants、低 reasoning effort、Responses API 细节；上游 Copilot SDK/v2 provider 迁入 core | GitHub Copilot GPT-5 | 部分已有；当前有 Copilot SDK 和测试，但上游 Copilot SDK 与 Aether 本地结构差异更大 | 暂缓，单独评估 Copilot | Copilot SDK 自维护成本高，不能混在 provider 通用回迁里 | 跑 `test/provider/copilot/**`，必要时补 GPT-5 variant cases | `cd packages/opencode && bun test test/provider/copilot` |
| LLM-UP-013 | Local | diff-only | OpenAI-compatible `includeUsage`、`max_tokens` 到 `max_completion_tokens`、reasoning 回灌、LiteLLM `_noop`、SSE timeout、HTTP proxy | OpenAI-compatible、LiteLLM、国内网关 | Aether 本地能力，上游没有等价或策略不同 | 必须保留 | 回迁上游 `provider.ts`/`transform.ts` 时最容易被覆盖 | 保留现有 provider/transform/LLM smoke 测试 | `cd packages/opencode && bun test test/provider/provider.test.ts test/provider/transform.test.ts test/session/llm.test.ts` |
| LLM-UP-014 | P2 | 多个 Effect/HttpApi/config 拆分 commit，如 `3df18dcde`、`e8471256f`、`ee7339f2c`、`93940a185` | Provider/Session/Config Effect service 化、HttpApi 化、module barrel 删除 | 架构层 | 与本轮 LLM 行为修复耦合低 | 放弃本轮回迁 | 大面积覆盖会吞掉 Aether 本地改动，且测试面过大 | 不做 | 不适用 |
| LLM-UP-015 | P1 | local-only（`provider/sdk/copilot/**`，引入自 `d9f18e400`；触发面疑似随 `2046ce0d6` 工具工厂迁移变大）；上游后续 `834515231` / `#25821` | Copilot V2 SDK 两处缺陷：① `chat/openai-compatible-chat-language-model.ts` 对首个 tool-call delta 缺 `id` 的解析过严，抛 `InvalidResponseDataError: Expected 'id' to be a string.`（OpenAI-compatible 协议允许后续 chunk 才补 id/name，是 SDK 实现的兼容缺陷）；② SDK 整体仍实现 `LanguageModelV2`，AI SDK v6 `streamText` 因 `specificationVersion: "v3"` 跳过 `wrapLanguageModel`，`ProviderTransform.message` 与 finish-reason 归一对 Copilot chat 路径不生效（已在 `session/llm.ts` 注释存档）；上游已转向 core/v2 provider 结构 | GitHub Copilot chat 路径（非 Responses） | 未覆盖；触发后会直接中断 tool 调用（如 `memory_list`）；上游新结构不能直接套用 | 短期：放宽 chat 首 delta 解析——id/name 同时缺失时占位等下一帧，仅在半残（id 与 name 仅缺其一）时仍抛；长期：把 Copilot SDK 升到 `LanguageModelV3` 或重评 core/v2 路线 | 短期改动需保证不破坏现有 chat 单测；V3/core 迁移涉及面大、需独立专项 | 复现 chat 路径 tool-call streaming 即可触发；补 chat 适配器单测覆盖"首 delta 仅含 index"分支 | `cd packages/opencode && bun test test/provider/copilot` |
| LLM-UP-016 | P0 | `25ecf0af6` / `#25888` | `server_is_overloaded` stream error 应按 server error retry | OpenAI-compatible、OpenAI、代理网关流式错误 | 未覆盖；当前 `ProviderError.parseStreamError` 未见 `server_is_overloaded` case | 移植 | 不处理会把可重试过载错误当未知/不可重试错误，中断真实 provider 调用 | 补 stream error parser 单测，确认 `server_is_overloaded` 与 `server_error` 都 retry | `cd packages/opencode && bun test test/session/llm.test.ts` |
| LLM-UP-017 | P0 | `ca77b8f8e` / `#25573` | Cloudflare AI Gateway `ai-gateway-provider` 的 providerOptions 应走 `openaiCompatible` key | Cloudflare AI Gateway / `ai-gateway-provider` | 未覆盖；当前 `ProviderTransform.sdkKey()` 未映射 `ai-gateway-provider`，本地已有 Cloudflare loader | 移植 | reasoning/cache 等 providerOptions 可能被放到 `cloudflare-ai-gateway` key 下并被 SDK 忽略 | 补 `providerOptions` namespace 单测，覆盖 `reasoningEffort` 进入 `openaiCompatible` | `cd packages/opencode && bun test test/provider/transform.test.ts` |
| LLM-UP-018 | P0 | `e76cf967e` / `#27254` | 中断时 finalize 空 assistant，避免未完成 assistant 污染后续 loop | Session prompt/cancel/tool loop | 未覆盖；当前未见等价 `Effect.onInterrupt` finalize 逻辑 | 手工改写 | 未完成空 assistant 可能导致后续消息 parent/状态异常，或留下 poison message | 补 cancel race 测试，覆盖 processor 创建前/后中断都写入 `MessageAbortedError` 与 completed time | `cd packages/opencode && bun test test/session/prompt.test.ts` |
| LLM-UP-019 | P0 | `233fc5b91` / `#21370`、`4e14f7951` / `#26276` | Anthropic/Bedrock signed reasoning replay：保留 signed reasoning 的空分隔 text，避免过滤带 signature/redactedData 的空 reasoning | Anthropic、Bedrock Claude、Google Vertex Anthropic | 未完整覆盖；当前只过滤空 text/reasoning，未明显保留 signed reasoning separator | 手工改写 | 历史消息 replay 时 signed reasoning 位置改变，可能触发 Anthropic/Bedrock API 校验错误 | 补 `message-v2` 与 transform 单测，覆盖 anthropic/bedrock signature、redactedData、空 text separator | `cd packages/opencode && bun test test/session/message-v2.test.ts test/provider/transform.test.ts` |
| LLM-UP-020 | P1 | `c36ab3f93` / `#26279` | Gemini thinking controls 细分：Gemini 2.5 Pro max budget、Gemini 3 flash/image/pro-image effort set、smallOptions | Google Gemini、Google Vertex Gemini | 部分覆盖；当前已有基础 `thinkingConfig`，但 Gemini 3 flash/image/pro-image 与 2.5 Pro max budget 不完整 | 手工改写或暂缓 | 错误 thinking level/budget 可能导致部分 Gemini 模型请求失败或 variant 无效 | 跟随 provider transform 测试补齐各 Gemini 2.5/3 variant | `cd packages/opencode && bun test test/provider/transform.test.ts` |
| LLM-UP-021 | P0/P1 | `967557979` / `#26222` | Azure `gpt-5.5` completions API 避免发送 `reasoningEffort`，只保留 `reasoningSummary:auto` | Azure OpenAI `gpt-5.5` completions/chat compatibility | 部分覆盖；Aether 有 Azure reasoning scrub 与本地 `gpt-5.x` 逻辑，但需按此 PR 专门复核 | 复核后决定移植/已覆盖 | Azure completions API 对 `reasoningEffort` 敏感，可能直接 400 | 补 Azure `gpt-5.5` completions contract case | `cd packages/opencode && bun test test/provider/transform.test.ts test/session/llm.test.ts` |
| LLM-UP-022 | P1 | open PR `#26892` | strip non-latest assistant reasoning blocks to fix compaction | Compaction、历史 reasoning replay | 未合并；只作为上游风险信号 | 跟踪 | Open PR 未稳定，不应直接回迁；但说明 compaction + reasoning 历史仍有风险 | 暂不补代码；后续 PR 合并后再抽行为 | 不适用 |
| LLM-UP-023 | P1/P2 | `36d40fee4` / `#26644` | session usage totals 落库与投影累计 | Session usage/statistics | 非本轮 LLM 调用链 P0；Aether 现有 usage 归一仍按 message/step 计算 | 暂缓 | 涉及 schema/migration/projector，回迁成本高 | 如后续做 usage UI/统计再评估 | 不适用 |
| LLM-UP-024 | P1 | `ca28dd02e` / `#27145` | compaction summarization 后恢复 tail turns | Session compaction | 重要但偏 compaction 语义，需与 Aether 当前 compaction-flow 测试对齐 | 单独评估 | 可能影响长会话续写上下文与 summary/tail 边界 | 跑 compaction/flow 测试并补 tail_start/include 语义断言 | `cd packages/opencode && bun test test/session/compaction.test.ts test/session/compaction-flow.test.ts` |
| LLM-UP-025 | P1/P2 | `20cec9155` / `#27372`、`9818c9e8` / `#27405` | provider model suggestions 恢复、小模型 fallback 无效配置降级 | Provider lookup / small model | 非真实 provider 调用失败；偏 UX 和 fallback 韧性 | 降级处理 | 价值存在，但不应抢占 P0 provider 调用修复 | 后续 provider UX 专项补 `provider.test.ts` | `cd packages/opencode && bun test test/provider/provider.test.ts` |

## 优先回迁建议

当前 HEAD 复核后，第一批 P0 行为已经由现有工作区实现或本地实现覆盖：

1. `LLM-UP-001` 到 `LLM-UP-006`：标记为已覆盖并已用本地测试验证。
2. `LLM-UP-007`：已随 AI SDK v6 专项升级迁移，仍建议真实 OpenRouter/DeepSeek smoke。
3. `LLM-UP-008`：维持本地 Azure providerOptions 策略，但 `#26222` 的 `gpt-5.5` completions API 修复需按 `LLM-UP-021` 复核。
4. `LLM-UP-009`：已随 AI SDK v6 专项重评，usage/compaction 本地测试通过。
5. `LLM-UP-010` 到 `LLM-UP-012`：P1 保持专项评估或本地策略优先，不做本轮代码回迁。
6. 2026-05-04 之后新增第一批优先复核：`LLM-UP-016`、`LLM-UP-017`、`LLM-UP-018`、`LLM-UP-019`、`LLM-UP-021`。
7. `LLM-UP-020` 可跟随 provider transform 测试补齐；`LLM-UP-022` open PR 与 `LLM-UP-023`/`LLM-UP-024`/`LLM-UP-025` 暂不直接回迁。

`LLM-UP-007` 不建议进入第一批，因为它牵涉 `@openrouter/ai-sdk-provider`、AI SDK peer dependency、`bun.lock` 和现有 patch。应作为依赖升级专项处理。

## 验收建议

第一批 P0 复核后从 package 目录运行：

```bash
cd /home/fyl/opencode/packages/opencode
bun typecheck
bun test test/provider/transform.test.ts
bun test test/provider/provider.test.ts
bun test test/session/message-v2.test.ts
bun test test/session/prompt.test.ts
bun test test/session/retry.test.ts
bun test test/session/llm.test.ts
```

若声称修复真实 provider：

```bash
cd /home/fyl/opencode/packages/opencode
OPENCODE_SYSTEM_TEST=1 bun run test:system:llm:p0 -- --provider deepseek
OPENCODE_SYSTEM_TEST=1 bun run test:system:llm:p0 -- --provider alibaba-cn --input p1-reasoning --p1
OPENCODE_SYSTEM_TEST=1 bun run test:system:llm:p0 -- --provider minimax-cn
```

新增 2026-05-14 复核项若声称修复真实 provider，建议补跑：

- Azure `gpt-5.5` completions/chat compatibility smoke。
- Cloudflare AI Gateway smoke。
- Anthropic/Bedrock signed reasoning replay smoke。
- Gemini 2.5/3 reasoning variant smoke。

## 结论

当前仓库与上游 `dev` 的 LLM call chain 差异主要由上游 Effect/HttpApi 架构迁移、AI SDK v6/provider SDK 升级和少量真实 provider bugfix 混合造成。本轮不应整块覆盖上游文件。

当前 HEAD 已覆盖并验证 `LLM-UP-001` 到 `LLM-UP-006` 的第一批 P0 行为，并明确保留 `LLM-UP-013` 的 Aether 本地兼容层。依赖升级、models.dev 服务化、Copilot SDK 和 Effect 架构迁移应分别开专项评估。

## 后续更新

报告主体写于 HEAD `b2f150759`。之后落地的相关 commit：

- `2046ce0d6 chore(opencode): 升级 AI SDK 至 v6 并回迁 LLM 调用链修复`
  - 兑现 `LLM-UP-007`（OpenRouter）与 `LLM-UP-009`（Anthropic/Bedrock usage）的依赖升级路径
  - 副作用：Copilot SDK 仍是 V2，v6 `streamText` 因 `specificationVersion: "v3"` 跳过 `wrapLanguageModel`——见新增的 `LLM-UP-015`
- `bafdfb6d2 fix(opencode): 修复 session.diff 去重与 providerExecuted 过滤失效`
  - 修 `b2f150759` / `2046ce0d6` 在 `session/summary` 与 `prompt` 路径上引入的两类问题（`session.diff` 去重不正确导致前端闪烁；`providerExecuted` 过滤死代码导致 Copilot provider-executed 工具多转一圈）
  - 与 `LLM-UP-015` 不重叠：本 commit 修的是 `prompt.ts` 的语义判断与 `processor.ts` 的字段透传，不触碰 Copilot SDK 内部解析

新增 `LLM-UP-015`：Copilot V2 SDK 的 chat 适配器对首个 tool-call delta 缺 `id` 抛错，以及整套 V2 SDK 让 v6 中间件失效两件事。触发现场：`memory_list` 等工具调用走 Copilot chat 路径时直接 abort。

报告基线（HEAD/未触碰列表）保留原状作为历史快照，后续只追加更新段落。

## 2026-05-14 复核补充

本次只更新报告，不改运行时代码。

复核基线：

- 当前仓库：`/home/fyl/opencode`
- 当前分支：`feat/llm-system-smoke-tests-squashed`
- 当前 HEAD：`56e4d03d9`
- 上游仓库：`/home/fyl/tmp/opencode`
- 上游分支：`dev`
- 上游 HEAD：`27ac53aaa`
- 当前 Aether 分支已包含后续 LLM 相关提交：`2e78a8381`、`5489ee795`、`56e4d03d9`

复核方法：

- 用本地上游 clone 扫描 `2026-05-04` 之后触及 `packages/opencode/src/session`、`packages/opencode/src/provider`、`packages/opencode/test/session`、`packages/opencode/test/provider`、`packages/opencode/package.json`、`bun.lock` 的提交。
- 用 `gh pr view` 核对报告已有 PR 与新增 PR 的合并状态、影响文件和标题。
- GitHub issue 复杂 OR 搜索在当前 `gh`/API 组合下返回不稳定，本轮以已合并 PR、open PR、本地 commit/diff 为主要证据。

旧项状态更新：

- `LLM-UP-007`、`LLM-UP-009`：仍按“已随 AI SDK v6 回迁，仍需真实 provider smoke”处理。
- `LLM-UP-008`：追加 `#26222` Azure `gpt-5.5` completions API 修复，归入新增 `LLM-UP-021` 单独复核。
- `LLM-UP-010`：上游 `#27347` 已把 `models.dev` 移入 `packages/core`，继续判定为架构迁移噪音，不整块回迁。
- `LLM-UP-012`、`LLM-UP-015`：上游 `#25821` 将 Copilot SDK/v2 provider 迁入 core，Aether 本地 Copilot SDK 与上游差异扩大，继续单独专项。

新增重点：

- 第一批新增 P0/P0-P1：`LLM-UP-016`、`LLM-UP-017`、`LLM-UP-018`、`LLM-UP-019`、`LLM-UP-021`。
- 可跟随 provider transform 专项补齐：`LLM-UP-020`。
- 仅跟踪或降级处理：`LLM-UP-022`、`LLM-UP-023`、`LLM-UP-024`、`LLM-UP-025`。

## 2026-05-14 实施更新

本次更新已落地代码与测试，不再只是分析报告。

已回迁并通过本地契约测试：

- `LLM-UP-016`：`ProviderError.parseStreamError()` 已支持 `server_error` / `server_is_overloaded`，并支持 OpenAI stream error 常见的 `{ message: JSON.stringify(errorChunk) }` 外层形态。覆盖测试：`test/session/message-v2.test.ts`、`test/session/retry.test.ts`。
- `LLM-UP-017`：`ai-gateway-provider` 的 `providerOptions` 已映射到 AI SDK `openaiCompatible` namespace。覆盖测试：`test/provider/transform.test.ts`；并新增 `test/provider/cf-ai-gateway-e2e.test.ts` 验证 `reasoning_effort` 进入 Cloudflare AI Gateway upstream wire payload。
- `LLM-UP-018`：当前结构下不迁移上游 Effect harness，但已补本地真实 prompt loop cancel 覆盖：hanging OpenAI-compatible stream 在 `SessionPrompt.cancel()` 后返回 assistant，写入 `MessageAbortedError` 与 `time.completed`。已有 `SessionRecovery.repairSession()` 覆盖无 active run 时修复未完成 assistant。覆盖测试：`test/session/prompt.test.ts`。
- `LLM-UP-019`：Anthropic signed reasoning replay 已补：Anthropic signed reasoning 间的空 text separator 转为空格；Anthropic/Bedrock 带 `signature` 或 `redactedData` metadata 的空 reasoning part 不再被过滤。覆盖测试：`test/session/message-v2.test.ts`、`test/provider/transform.test.ts`。
- `LLM-UP-021`：Azure `gpt-5.5` completions API 不再发送 `reasoningEffort`，同时保留当前 Aether 策略：Azure providerOptions 使用 `azure` key，不 remap 到 OpenAI。覆盖测试：`test/provider/transform.test.ts`。
- LLM stream abort 资源释放：新增 `LLM.stream` abort 后 provider response body 及时 cancel 的本地测试。覆盖测试：`test/session/llm.test.ts`。

新增回迁候选并已处理：

| 编号 | 优先级 | 上游 commit/PR | 变更主题 | 影响 provider/model | 当前 Aether 状态 | 建议动作 | 风险 | 测试要求 | 验收命令 |
|------|--------|----------------|----------|---------------------|------------------|----------|------|----------|----------|
| LLM-UP-026 | P0/P1 | `6409aceb1` / `#25934` | 清洗孤立 UTF-16 surrogate，避免非法文本进入 provider request | 所有 provider，尤其包含文件/tool/shell 输出的请求 | 已回迁；`ProviderTransform.message()` 会清洗 system/user/assistant/reasoning/tool-result 中模型可见文本，保留合法 surrogate pair | 已验证 | 需避免改动 image 等非文本内容；当前测试覆盖合法 emoji 保留 | 新增 surrogate sanitization 单测 | `cd packages/opencode && bun test test/provider/transform.test.ts` |
| LLM-UP-027 | P0/P1 | `c1f607d20` / `#25721` | Azure SDK model selector 在 `responses` 缺失时 fallback 到 `messages` / `chat` / `languageModel` | Azure OpenAI、Azure Anthropic/兼容 SDK 形态 | 已回迁；`Provider.azureLanguage()` 统一选择 Azure model endpoint，`useCompletionUrls` 仍优先 `chat` | 已验证 | 只改 SDK method selection，不改变 Azure namespace 或 request option 策略 | 新增 Azure selector 单测 | `cd packages/opencode && bun test test/provider/provider.test.ts` |

本轮明确没有做的项：

- `LLM-UP-020` Gemini thinking controls：仍按 P1 处理，后续跟 provider transform/Gemini 专项。
- `LLM-UP-024` compaction tail restore：重要但属于 compaction 语义批次，不并入本轮 LLM API P0。
- `83bb21648` tools 排序稳定、`75d141b57` subtask child session cancel：属于 P1 稳定性候选；如后续做 tool/subtask cancel 专项，再抽行为补本地测试。
- Copilot V2 SDK `LLM-UP-015`：仍是独立专项，不能混入 provider 通用回迁。

本轮本地验收：

```bash
cd /home/fyl/opencode/packages/opencode
bun typecheck
bun test test/provider/provider.test.ts test/provider/transform.test.ts test/provider/cf-ai-gateway-e2e.test.ts test/session/message-v2.test.ts test/session/retry.test.ts test/session/llm.test.ts test/session/prompt.test.ts
```

当前已知 P0/P0-P1 调用链风险在本地契约测试层面已处理完毕。真实 provider 兼容性仍建议按风险点分别补 smoke：Azure `gpt-5.5` completions/chat、Cloudflare AI Gateway、Anthropic/Bedrock signed reasoning replay、OpenAI stream overload retry。
